{
  "name": "incidentlite-api",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "func start",
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "dev": "npm run build:watch & npm run start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@incidentlite/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.2.2"
  }
}
